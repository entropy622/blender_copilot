[project]
name = "blender-copilot"
version = "0.4.0"
description = "Blender 5.2 add-on with built-in HTTP MCP and Material Graph Code"
requires-python = ">=3.11"
license = "MIT"
dependencies = []

[project.optional-dependencies]
test = ["mcp>=1.30,<2", "httpx>=0.27,<1"]

[tool.setuptools]
packages = []
